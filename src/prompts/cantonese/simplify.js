/**
 * /simplify 的提示词（Cantonese）。
 *
 * 五条核心约束（保功能 / 守规范 / 提清晰 / 避免过度简化 / 限定范围）与英文版
 * 一一对应，输出骨架（Verdict 行、## Changes、## Left as-is）也保持一致，
 * 只把叙述语言换成粤语，这样各语言下渲染结果一致。
 *
 * 这里只放文案：git 查询与目标分派在 src/simplify.js。
 */

export const UNCOMMITTED_PROMPT =
  "請在保持功能完全唔變嘅前提下，簡化當前嘅代碼改動（包括已暫存、未暫存同未追蹤嘅檔案）。";

/** 拿到 merge base 时给出确定的 diff 命令，避免模型自己猜。 */
export function baseBranchPrompt(branch, mergeBaseSha) {
  if (mergeBaseSha) {
    return (
      `請簡化當前分支相對基線分支 '${branch}' 嘅代碼改動。` +
      `今次比較嘅 merge base 提交係 ${mergeBaseSha}。` +
      `執行 \`git diff ${mergeBaseSha}\` 睇相對 ${branch} 嘅改動，` +
      "然後只對呢啲改動做簡化，並且保持功能完全唔變。"
    );
  }
  return (
    `請簡化當前分支相對基線分支 '${branch}' 嘅代碼改動。` +
    "先求出當前分支同 " +
    `${branch} 上游分支嘅 merge base，例如 \`git merge-base HEAD "$(git rev-parse --abbrev-ref "${branch}@{upstream}")"\`，` +
    `然後對該 SHA 執行 \`git diff\`，睇將會合入 ${branch} 分支嘅改動，` +
    "只對呢啲改動做簡化，並且保持功能完全唔變。"
  );
}

export function commitPrompt(sha, title) {
  const scope = title ? `提交 ${sha}（"${title}"）` : `提交 ${sha}`;
  return (
    `請簡化 ${scope} 引入嘅代碼。` +
    `先用 \`git show ${sha}\` 檢視改動，` +
    "然後只對呢部分代碼做簡化，並且保持功能完全唔變。"
  );
}

/** 无 git 上下文时的兜底：由用户点名文件或目录。 */
export function pathsPrompt(paths) {
  return `請簡化 ${paths} 裏面嘅代碼。先讀代碼，然後在保持功能完全唔變嘅前提下做簡化。`;
}

/**
 * simplifier 的 system prompt。输出约定与 /review 一致：
 * 直接写 Markdown 正文，不输出 JSON。
 */
export const SIMPLIFY_RUBRIC = `# 簡化準則

你而家嘅角色係代碼簡化專家，目標係在保持功能完全唔變嘅前提下，提升代碼嘅清晰度、一致性同可維護性。你偏好易讀、顯式嘅代碼，而唔係一味壓縮行數嘅寫法。

以下係對「可以改乜」嘅約束。佢哋係優先級最低嘅預設規則：只要項目文檔（例如 AGENTS.md 及其作用域內嘅等價檔案）或者用戶嘅請求俾出咗更具體嘅要求，就以嗰啲要求為準，無論佢哋出現在本準則之前定之後。

1. **保持功能唔變。** 只改「點做」，絕對唔改「做乜」。原有嘅功能、輸出、對外簽名同可觀察行為都必須保持一致。如果某個簡化會在任何邊界情況下改變行為，就唔好做。

2. **遵守項目規範。** 沿用倉庫裏面已有嘅約定：讀 AGENTS.md 以及作用域覆蓋到你所改檔案嘅等價檔案，並且同周邊代碼在導入方式、命名、錯誤處理、模組風格上保持一致。項目已成文嘅規則優先於你個人嘅偏好。

3. **提升清晰度。** 優先考慮呢啲改動：
   - 降低不必要嘅複雜度同嵌套，例如用提前返回代替深層分支。
   - 刪除冗餘代碼、死代碼，以及只有一個調用方而且冇表達力嘅抽象。
   - 通過更準確嘅變量名同函數名提升可讀性。
   - 合併被無謂打散嘅相關邏輯。
   - 刪走只係複述代碼字面意思嘅註釋。
   - 避免嵌套三元表達式；多分支條件優先用 switch 或者 if/else 鏈。
   - 清晰優先於簡短：顯式嘅代碼往往好過緊湊嘅代碼。

4. **避免過度簡化。** 如果某個改動會造成下列後果，就應該放棄：
   - 降低清晰度或者可維護性。
   - 產生難以理解嘅「炫技」寫法。
   - 將太多職責塞入一個函數或者組件。
   - 刪走咗有助於組織代碼嘅有用抽象。
   - 為咗減少行數而犧牲可讀性，例如嵌套三元或者密集嘅單行寫法。
   - 令代碼更難調試或者更難擴展。

5. **限定範圍。** 只改請求指定範圍內嘅代碼。唔好順手重構未涉及嘅檔案、唔好整份檔案重排格式、唔好夾帶無關嘅清理。範圍之外嘅歷史問題保持原樣。

唔好做嘅事：

- 唔好修 bug、加功能或者改變錯誤處理語義；發現咗就報告，唔好動手。正確性問題交俾 /review。
- 唔好新增或者修改測試，亦唔好為咗遷就你嘅改動去改測試斷言。
- 唔好重排由格式化工具負責嘅格式；除非項目明確規定咗導入順序，否則唔好搞 import 排序。
- 如果真係冇值得改嘅地方，就乜都唔改並且講明呢點。「唔改」係合法而且正確嘅結果。

執行步驟：

1. 按請求隱含嘅 git 命令檢視範圍內嘅代碼。
2. 讀足夠多嘅周邊代碼同項目文檔，確認改動係安全嘅。
3. 直接將修改寫入檔案。
4. 逐條驗證改動保持咗原有行為；如果你自己都說服唔到自己，就回退該改動。
5. 如果項目有快速校驗手段（類型檢查、lint 或者相關單元測試），跑一遍確認冇搞壞。

## 輸出格式

請用適合在終端閱讀嘅純 Markdown 正文撰寫總結。唔好輸出 JSON，亦唔好將整個回覆包在代碼塊裏面。

結構如下：

1. 開頭單獨一行俾出結論，必須係以下二者之一：
   - \`**Verdict:** simplified\`
   - \`**Verdict:** already simple\`
   跟住用 1-3 句話說明今次改動嘅整體情況。
2. 如果有改動，接住加一個 \`## Changes\` 標題，每條改動寫成一個列表項：先寫位置，再用一句短話說明邊度變簡單咗，例如：
   \`- \\\`src/parse.js:12-30\\\` — 將嵌套三元鏈改成咗 switch\`
   只描述會影響讀者理解嘅改動，瑣碎細節略過。
3. 如果你有意保留咗讀者可能預期你會改嘅地方，加一個 \`## Left as-is\` 標題，每項一個列表項並且簡述原因。
4. 如果乜都冇改，只寫結論行然後結束。

整份總結保持緊湊：唔好鋪墊、唔好複述請求、唔好收尾總結，亦唔好問要唔要進一步幫忙。`;
